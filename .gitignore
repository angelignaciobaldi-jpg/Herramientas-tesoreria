# ===== Python =====
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ===== Empaquetado (PyInstaller / flet pack) =====
build/
dist/
*.spec
# Chromium de Playwright descargado por construir.bat (pesado; se baja al build)
_browsers/
# Salida del instalador de Inno Setup
Output/
# Paquete generado para compartir (preparar_paquete.bat) [obsoleto]
# Chromium de Playwright descargado por construir.bat (pesado; se baja al build)
_browsers/
# Salida del instalador de Inno Setup
Output/
# Paquete generado para compartir (preparar_paquete.bat) [obsoleto]
Tesoreria-paquete/

# ===== Datos en runtime (se generan/actualizan; no van al repo) =====
tesoreria.db
*.db
_cuentas_cache.json
# Preferencias locales (estado de ventana, etc.) por máquina
preferencias.json
# Diagnósticos del RPA (capturas + HTML del portal real; datos sensibles)
_diagnostico_rpa/
# Credenciales del SIPP cifradas por usuario (DPAPI)
credenciales_rpa.json

# ===== Secretos / configuración sensible =====
# Variables de entorno con tokens (PAT de GitHub, etc.). El .env.example sí va.
.env

# ===== Modelos de OCR (pesados; se obtienen aparte) =====
tessdata/
# Motor Tesseract empaquetado para el instalador (lo prepara el CI; pesado)
tesseract_bundle/

# ===== Datos sensibles y archivos de ejemplo (NO subir: datos reales) =====
# Estados de cuenta / carátulas con nombres y CLABE de personas
CARATULAS/
# Excel con las cuentas bancarias de las empresas
Cuentas bancarias/
# Reporte de cuentas para conciliar (CLABE/nombres/correos reales de proveedores)
Reporte Cuentas Bancarias.xlsx
Reporte*.xlsx
# Documentos de prueba (estados de cuenta / cartas reales con datos sensibles)
Documentos de pruebas conciliación/
# Páginas HTML de referencia del portal/reportes (datos reales)
PAGINAS HTML/
# Archivos TXT de ejemplo (contienen CLABE/nombres reales)
Archivos TXT/
ALTABANREGIO 1.xls
Codigo macro excel
dispersion_prueba.txt
reporte_devoluciones.xlsx

#descargas del rpa
descargas/